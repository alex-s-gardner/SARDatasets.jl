var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SLCDatasets","page":"Home","text":"Documentation for SLCDatasets.\n\n","title":"SLCDatasets"},{"category":"module","location":"#SLCDatasets.SLCDatasets","page":"Home","text":"SLCDatasets\n\nRead single-look complex SAR products into one type, whatever the sensor.\n\nopen_slc returns an SLC carrying an Identification and a RadarGeometry — the slant-range/azimuth geometry in the units a geometry kernel wants — plus a StateVectors record, read on first access.\n\nThe scope is SLCs in radar geometry, not SAR products generally. An interferogram or a covariance product carries a multilooked grid, and a geocoded SLC carries map coordinates rather than a slant-range axis — neither is described by RadarGeometry, so neither is read here.\n\nNISAR-format HDF5 range-Doppler products (RSLC) are read natively. The metadata of one is a few tens of kilobytes near the start of a file tens of gigabytes long, so a remote product can be opened without transferring it.\n\nSentinel-1 IW SLCs are read from a .SAFE directory or the zip of one, taking geometry from the annotation XML and state vectors from a POEORB or RESORB .EOF file, which a Sentinel-1 product does not carry. A geometry costs the annotation alone, so a zip need not be unpacked to read one. A TOPS product is three subswaths of bursts rather than one image; open_slc describes either the mosaic across them or one individual burst.\n\nAn annotation document is a couple of megabytes holding a few dozen scalars, so a product's subswaths are parsed once into a Sentinel1Product and every geometry, burst and orbit derived from it reads nothing further. bursts returns a whole subswath's bursts as an SLCSeries — an AbstractVector of SLCs over that one parse, rather than one open_slc and one parse per burst.\n\nmerge_bursts folds those bursts into one acquisition, placing them on a single uniform azimuth grid so that a line's time is sensing_start + line / prf throughout. pixels and amplitude read its samples as they are indexed, and validmask says which of them were imaged. Samples need an unpacked .SAFE: inside a zip the raster is deflated, so a line is not addressable without inflating everything before it.\n\n\n\n\n\n","title":"SLCDatasets.SLCDatasets"},{"category":"constant","location":"#SLCDatasets.ASF_BURST_HOST","page":"Home","text":"ASF_BURST_HOST\n\nThe host ASF's burst extractor serves from.\n\n\n\n\n\n","title":"SLCDatasets.ASF_BURST_HOST"},{"category":"constant","location":"#SLCDatasets.DEFAULT_PREFETCH","page":"Home","text":"DEFAULT_PREFETCH\n\nBytes of a remote product fetched up front, 8 MiB.\n\nLarge enough for every NISAR product measured, and small enough to be one request. Raise it with RemoteHTTP(url; prefetch = ...) if a product's metadata lies further in.\n\n\n\n\n\n","title":"SLCDatasets.DEFAULT_PREFETCH"},{"category":"constant","location":"#SLCDatasets.MAX_BURST_GRID_RESIDUAL","page":"Home","text":"MAX_BURST_GRID_RESIDUAL\n\nHow far a burst's start may fall from the uniform azimuth axis before a merge is refused, in lines.\n\nA burst is placed at a whole-row offset, so a start lying between rows would shift its samples in azimuth by the difference. Every granule measured places within 2.2e-4 lines; this bound is two orders of magnitude above that and still far below the half-line where a placement would round the other way.\n\n\n\n\n\n","title":"SLCDatasets.MAX_BURST_GRID_RESIDUAL"},{"category":"type","location":"#SLCDatasets.AbstractBurstBackend","page":"Home","text":"AbstractBurstBackend <: AbstractSLCBackend\n\nOne burst of a TOPS acquisition, however it was delivered.\n\nA Sentinel-1 product is distributed both as a whole .SAFE, whose bursts are stacked in one raster per subswath, and as one file per burst. Both describe the same thing, so a backend of either kind answers the same five questions — burst_index, burst_swath, burst_polarization, burst_source and orbit_path — and merging asks those rather than naming the formats it knows.\n\n\n\n\n\n","title":"SLCDatasets.AbstractBurstBackend"},{"category":"type","location":"#SLCDatasets.AbstractSLC","page":"Home","text":"AbstractSLC\n\nOne single-look complex acquisition, whatever the sensor. See SLC.\n\n\n\n\n\n","title":"SLCDatasets.AbstractSLC"},{"category":"type","location":"#SLCDatasets.AbstractSLCBackend","page":"Home","text":"AbstractSLCBackend\n\nHow one sensor's bytes are laid out and named. A backend knows the group and dataset paths of its product family; it does not know how the bytes are reached — that is an AbstractSLCSource.\n\n\n\n\n\n","title":"SLCDatasets.AbstractSLCBackend"},{"category":"type","location":"#SLCDatasets.AbstractSLCSource","page":"Home","text":"AbstractSLCSource\n\nWhere a product's bytes come from. LocalFile is the only route that needs no network; see RemoteHTTP and RemoteS3 for the others.\n\n\n\n\n\n","title":"SLCDatasets.AbstractSLCSource"},{"category":"type","location":"#SLCDatasets.Amplitude","page":"Home","text":"Amplitude{T,P} <: AbstractMatrix{T}\n\nThe magnitudes of a complex array, taken as they are read.\n\nHolds no samples of its own, so wrapping a whole subswath costs nothing and a window taken from it costs the window. eltype is Float32: the samples are 16-bit integers, whose magnitude is exact in it, and it is what a correlator works in.\n\n\n\n\n\n","title":"SLCDatasets.Amplitude"},{"category":"type","location":"#SLCDatasets.AnnotationTree","page":"Home","text":"AnnotationTree\n\nThe annotation/ entries of an open SAFE container, listed once.\n\nA zip must be memory-mapped and its central directory parsed to reach any entry, and a product's polarizations and its per-subswath annotations are both answered from that one listing. Holding it makes opening a product one traversal rather than one per query.\n\n\n\n\n\n","title":"SLCDatasets.AnnotationTree"},{"category":"type","location":"#SLCDatasets.AsfBurst","page":"Home","text":"AsfBurst(slc, swath, polarization, burst; dir = nothing) -> AsfBurst\n\nOne burst of a Sentinel-1 SLC, from ASF's burst extractor.\n\nslc is the parent granule's name, burst its 1-based index within the subswath — the extractor addresses bursts from zero and the conversion happens here, so that a burst has the same number it has everywhere else in this package.\n\nBoth files are fetched whole rather than by range: a burst raster is a couple of hundred megabytes and is wanted entire. dir is where they are kept, defaulting to a temporary directory; pass one to keep them across sessions, and an already-downloaded file there is not fetched again.\n\nAuthentication is from ~/.netrc, as for RemoteHTTP, so a urs.earthdata.nasa.gov entry is the only setup. The extractor generates a burst on first request and answers 202 until it is ready; this retries until it is.\n\nExamples\n\nb = AsfBurst(\"S1A_IW_SLC__1SSH_20151120T080202_...\", 2, \"HH\", 3)\ns = open_slc(b)\n\n\n\n\n\n","title":"SLCDatasets.AsfBurst"},{"category":"type","location":"#SLCDatasets.AsfBurstBackend","page":"Home","text":"AsfBurstBackend <: AbstractSLCBackend\n\nOne ASF-delivered burst: its own raster, and the annotation of the subswath it belongs to.\n\n\n\n\n\n","title":"SLCDatasets.AsfBurstBackend"},{"category":"type","location":"#SLCDatasets.BurstGrid","page":"Home","text":"BurstGrid\n\nThe shape of a merged subswath and the placement of every burst in it.\n\nBuilt by burst_grid. Holds no pixels and no times: it is the integer layout alone, so it can be checked against the arithmetic it reproduces without reading a raster.\n\nFields\n\nnlines, nsamples: the merged image's size.\nplacements: one per burst merged, in azimuth order, with disjoint grid_rows.\nresidual: how far the worst burst start fell from the shared azimuth axis, in lines. A measure of how well the uniform grid describes this product; a copy is exact only because it is small.\n\n\n\n\n\n","title":"SLCDatasets.BurstGrid"},{"category":"type","location":"#SLCDatasets.BurstPlacement","page":"Home","text":"BurstPlacement\n\nWhere one burst's valid region lands in a merged image.\n\ngrid_rows and burst_rows are the same length: row grid_rows[k] of the image is row burst_rows[k] of the burst. cols indexes both, since bursts of one subswath share a range origin.\n\n\n\n\n\n","title":"SLCDatasets.BurstPlacement"},{"category":"type","location":"#SLCDatasets.BurstRaster","page":"Home","text":"BurstRaster(raster, row_offset = 0)\n\nOne burst's samples: the raster holding them, and where in it the burst begins.\n\nA .SAFE stores a subswath's bursts stacked in one raster, so each burst begins at its own offset into it; a burst delivered as its own file begins at the start. Pairing the two makes those the same thing to a reader.\n\n\n\n\n\n","title":"SLCDatasets.BurstRaster"},{"category":"type","location":"#SLCDatasets.BurstValidMask","page":"Home","text":"BurstValidMask <: AbstractMatrix{Bool}\n\nWhich samples of a merged image were imaged, derived from the grid rather than stored.\n\ntrue exactly where ConcatenatedBursts reads from a burst's valid region. The image reads zero outside, but a sample inside can be zero too, so this is what distinguishes absent from dark and what a correlator should be given rather than a test against zero.\n\n\n\n\n\n","title":"SLCDatasets.BurstValidMask"},{"category":"type","location":"#SLCDatasets.ConcatenatedBursts","page":"Home","text":"ConcatenatedBursts{T,S} <: AbstractMatrix{T}\n\nThe bursts of one Sentinel-1 subswath as a single matrix, read on demand.\n\nRows come from whichever burst covers them under the array's BurstGrid; rows and samples no burst reaches read as zero(T). Build one with pixels on a merged SLC rather than calling this constructor.\n\nIndex with ranges — A[rows, cols] — wherever the shape of the read allows it: a window is taken as one strip per burst it spans, while scalar indexing resolves a row per sample.\n\n\n\n\n\n","title":"SLCDatasets.ConcatenatedBursts"},{"category":"type","location":"#SLCDatasets.Identification","page":"Home","text":"Identification\n\nWhat an acquisition is: mission, product, orbit and coverage.\n\nstart_time and stop_time are the zero-Doppler bounds. They are kept as the product's own strings because those carry nanoseconds, which DateTime's millisecond resolution would silently drop; start_datetime and stop_datetime convert when millisecond truncation is acceptable. The full-precision bounds the geometry uses are sensing_start and sensing_stop on RadarGeometry, in seconds against its epoch.\n\n\n\n\n\n","title":"SLCDatasets.Identification"},{"category":"type","location":"#SLCDatasets.LocalFile","page":"Home","text":"LocalFile(path)\n\nA product already on disk.\n\n\n\n\n\n","title":"SLCDatasets.LocalFile"},{"category":"type","location":"#SLCDatasets.LookSide","page":"Home","text":"LookSide\n\nWhich side of the flight track the radar points: LookLeft or LookRight.\n\n\n\n\n\n","title":"SLCDatasets.LookSide"},{"category":"type","location":"#SLCDatasets.MergedBurstBackend","page":"Home","text":"MergedBurstBackend <: AbstractSLCBackend\n\nSeveral bursts of one Sentinel-1 subswath, described as a single acquisition.\n\nBuilt by merge_bursts. Holds the parsed product, the subswath, and the layout the bursts were placed with, so the geometry and the samples both derive from one grid.\n\n\n\n\n\n","title":"SLCDatasets.MergedBurstBackend"},{"category":"type","location":"#SLCDatasets.NisarBackend","page":"Home","text":"NisarBackend <: AbstractSLCBackend\n\nA NISAR-format HDF5 product: a file, the sensor band group inside it, and the product type.\n\nfrequency selects the sub-band (\"A\" or \"B\"); a product lists the ones it carries at identification/listOfFrequencies.\n\n\n\n\n\n","title":"SLCDatasets.NisarBackend"},{"category":"type","location":"#SLCDatasets.RadarGeometry","page":"Home","text":"RadarGeometry\n\nThe slant-range/azimuth geometry of an acquisition: everything needed to relate a pixel index to a range and a time, in the units a geometry kernel wants.\n\nFields\n\nstarting_range, far_range: slant range to the first and last range sample, meters.\nrange_pixel_spacing: range sample spacing, meters.\nwavelength: radar wavelength, meters — c over the processed center frequency.\nprf: pulse repetition frequency, Hz, so 1/prf is the azimuth line spacing in time.\nsensing_start, sensing_stop: azimuth time of the first and last line, seconds since epoch.\nnlines, nsamples: image height and width in pixels — azimuth lines and range samples.\nlook_side: which side the radar points, read from the product rather than assumed.\nepoch: the instant sensing_start and the orbit's times are measured from.\n\nThe epoch\n\nepoch is the one clock this type reports against, and both the azimuth times and the orbit's state vector times are on it. A consumer measuring azimuth time against a different origin — seconds since midnight, say — needs the offset between the two, which is why epoch is carried rather than being folded into the times and discarded.\n\n\n\n\n\n","title":"SLCDatasets.RadarGeometry"},{"category":"type","location":"#SLCDatasets.RemoteHTTP","page":"Home","text":"RemoteHTTP(url; prefetch = DEFAULT_PREFETCH, dir = nothing)\n\nA product reachable over HTTP from a server honoring range requests.\n\nOnly the first prefetch bytes are fetched. dir is where the sparse local file goes, defaulting to a temporary directory; pass one to keep it across sessions.\n\nEarthdata URLs are authenticated from ~/.netrc and the redirect to the signed data URL is followed, so a urs.earthdata.nasa.gov entry there is all the setup needed.\n\n\n\n\n\n","title":"SLCDatasets.RemoteHTTP"},{"category":"type","location":"#SLCDatasets.RemoteS3","page":"Home","text":"RemoteS3(uri; prefetch = DEFAULT_PREFETCH, dir = nothing)\n\nA product in S3, addressed as s3://bucket/key.\n\nReads the same way RemoteHTTP does, through the bucket's HTTPS endpoint, and so needs credentials in the environment (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, and AWS_SESSION_TOKEN for the temporary credentials a DAAC issues) and to be running in the bucket's region. Requester-pays and cross-region access are refused by the endpoint, not here.\n\n\n\n\n\n","title":"SLCDatasets.RemoteS3"},{"category":"type","location":"#SLCDatasets.SLC","page":"Home","text":"SLC <: AbstractSLC\n\nOne SAR acquisition, read through a sensor backend.\n\nidentification and geometry are parsed when the product is opened: they are a few dozen scalars, and a product whose metadata cannot be read is not usable, so failing at open_slc is better than failing later. The state vectors are read on first access and then held, since a consumer touching only the geometry should not pay for them.\n\nBuild one with open_slc rather than calling this constructor.\n\nExamples\n\ns = open_slc(\"NISAR_L1_PR_RSLC_....h5\")\ns.geometry.starting_range   # already in hand\norbit(s)                    # read on first access\n\n\n\n\n\n","title":"SLCDatasets.SLC"},{"category":"type","location":"#SLCDatasets.SLCSeries","page":"Home","text":"SLCSeries{S} <: AbstractVector{S}\n\nSeveral acquisitions of one product addressed as a vector, built on request rather than up front.\n\nThe bursts of a Sentinel-1 subswath are the case this exists for: they share one parsed annotation, so indexing builds the SLC for that burst from metadata already in hand. Every AbstractVector operation works, and collect materializes them all.\n\nExamples\n\nb = bursts(p; swath = 2)\nlength(b)\nb[3]                    # built here, no re-reading of the product\nlast(b).geometry.prf\nfilter(s -> nlines(s) > 1000, b)\n\n\n\n\n\n","title":"SLCDatasets.SLCSeries"},{"category":"type","location":"#SLCDatasets.Sentinel1Backend","page":"Home","text":"Sentinel1Backend <: AbstractSLCBackend\n\nWhich part of a parsed Sentinel1Product an SLC describes.\n\nA mosaic spans every subswath the product was opened for, so it has no subswath or burst of its own and both are nothing. An individual burst names the subswath it belongs to and its own 1-based index.\n\n\n\n\n\n","title":"SLCDatasets.Sentinel1Backend"},{"category":"type","location":"#SLCDatasets.Sentinel1Product","page":"Home","text":"Sentinel1Product(path; orbit, polarization = nothing, swaths = 1:3)\n\nA Sentinel-1 TOPS product with its annotation already parsed.\n\nswaths names the subswaths to read; each is parsed once and held, so every geometry, burst and orbit derived from this product costs no further parsing of the container. Build the acquisitions with open_slc or bursts rather than reaching into the fields.\n\n\n\n\n\n","title":"SLCDatasets.Sentinel1Product"},{"category":"type","location":"#SLCDatasets.StateVectorTable","page":"Home","text":"StateVectorTable\n\nPlatform state vectors in ECEF against absolute UTC instants, as a product's orbit file records them.\n\nThe intermediate a reader produces before an epoch is chosen: StateVectors is the same vectors with their times reduced to seconds against one epoch. The three fields are index-matched.\n\n\n\n\n\n","title":"SLCDatasets.StateVectorTable"},{"category":"type","location":"#SLCDatasets.StateVectors","page":"Home","text":"StateVectors\n\nPlatform state vectors in ECEF, and the instant they are measured from.\n\ntime is seconds since epoch, position is meters and velocity meters per second. The times are not required to be uniformly spaced here — a consumer needing that must check, since whether it holds is a property of the product rather than of this type.\n\nThis is the product's own record, not an interpolator: a consumer wanting to evaluate the trajectory between samples builds one from these.\n\n\n\n\n\n","title":"SLCDatasets.StateVectors"},{"category":"type","location":"#SLCDatasets.StripedTiff","page":"Home","text":"StripedTiff{T} <: AbstractMatrix{T}\n\nThe raster of an uncompressed, one-line-per-strip TIFF, memory-mapped and addressed by line.\n\nIndexing reads from the mapping, so a window costs the lines it covers and nothing else. Build one with open_tiff, which checks the file has this layout.\n\nIndexing a range of lines and samples — t[rows, cols] — is the operation to prefer: it copies each line's run in one go, where scalar indexing pays the address arithmetic per pixel.\n\n\n\n\n\n","title":"SLCDatasets.StripedTiff"},{"category":"type","location":"#SLCDatasets.SubswathAnnotation","page":"Home","text":"SubswathAnnotation\n\nOne subswath's annotation, reduced to the geometry fields.\n\nEverything a burst needs that is shared across the subswath's bursts is held once here, so a burst costs no parsing of its own.\n\n\n\n\n\n","title":"SLCDatasets.SubswathAnnotation"},{"category":"type","location":"#SLCDatasets.TiffField","page":"Home","text":"TiffField\n\nOne entry of a TIFF image file directory: what the tag is, and where its values are.\n\nAn entry's last four bytes are either the values themselves or the offset to them, depending on their total size, so both readings are kept: payload is those bytes as an integer, and valueoffset is where they sit in the file. Which one applies is resolved when the values are read.\n\n\n\n\n\n","title":"SLCDatasets.TiffField"},{"category":"type","location":"#SLCDatasets.UtcTime","page":"Home","text":"UtcTime(datetime, seconds)\n\nA UTC instant to better than millisecond resolution: datetime truncated to the whole second, plus seconds of residual.\n\nThe residual is normally in [0, 1), but is allowed past one second so a time can be advanced by an arbitrary offset without renormalizing; seconds_between and _utc_string both handle that.\n\n\n\n\n\n","title":"SLCDatasets.UtcTime"},{"category":"method","location":"#SLCDatasets._utc_string-Tuple{SLCDatasets.UtcTime}","page":"Home","text":"_utc_string(t::UtcTime) -> String\n\nt as YYYY-MM-DDThh:mm:ss.ffffff, the form the products write.\n\nA residual carried past one second reads as the time it denotes rather than as an out-of-range field.\n\n\n\n\n\n","title":"SLCDatasets._utc_string"},{"category":"method","location":"#SLCDatasets.amplitude-Tuple{AbstractArray{<:Complex}}","page":"Home","text":"amplitude(A) -> Amplitude\namplitude(s::SLC) -> Amplitude\n\nThe magnitude of each sample, computed as it is read.\n\nThis is what the reference pipeline correlates: it takes abs of the samples and discards the phase, so a feature tracker sees magnitudes. The phase is still in the product — pixels returns it — for a consumer that correlates the complex samples instead.\n\nref = merge_bursts(bursts(safe; orbit = eof, swath = 2))\na = amplitude(ref)      # nothing read yet\na[1:512, 1:512]         # this window, and no more of the product\n\n\n\n\n\n","title":"SLCDatasets.amplitude"},{"category":"method","location":"#SLCDatasets.annotation-Tuple{Sentinel1Product, Integer}","page":"Home","text":"annotation(p::Sentinel1Product, swath) -> SubswathAnnotation\n\nThe parsed annotation of one subswath of p, which must be among the ones it was opened for.\n\n\n\n\n\n","title":"SLCDatasets.annotation"},{"category":"method","location":"#SLCDatasets.annotation_xml-Tuple{AbstractString, Any, AbstractString}","page":"Home","text":"annotation_xml(path, swaths, polarization) -> Vector{String}\n\nThe annotation XML of several subswaths, read in one pass over the container.\n\n\n\n\n\n","title":"SLCDatasets.annotation_xml"},{"category":"method","location":"#SLCDatasets.annotation_xml-Tuple{AbstractString, Integer, AbstractString}","page":"Home","text":"annotation_xml(path, swath, polarization) -> String\n\nThe text of the annotation XML for one subswath and polarization of a SAFE product.\n\n\n\n\n\n","title":"SLCDatasets.annotation_xml"},{"category":"method","location":"#SLCDatasets.asf_annotation-Tuple{AsfBurst}","page":"Home","text":"asf_annotation(b::AsfBurst) -> Node\n\nThe product annotation of the subswath a burst belongs to, from the burst's metadata file.\n\nThe file wraps a full annotation per subswath and polarization of the parent granule, each under a <content> element whose children are the sections a .SAFE annotation has at its root. So that element is the annotation, and what is returned lists every burst of the subswath rather than only this one.\n\n\n\n\n\n","title":"SLCDatasets.asf_annotation"},{"category":"method","location":"#SLCDatasets.asf_annotation_path-Tuple{AsfBurst}","page":"Home","text":"asf_annotation_path(b::AsfBurst) -> String\n\nThe local path of a burst's metadata file, fetching it if it is not already there.\n\n\n\n\n\n","title":"SLCDatasets.asf_annotation_path"},{"category":"method","location":"#SLCDatasets.asf_burst_url-Tuple{AsfBurst, AbstractString}","page":"Home","text":"asf_burst_url(b::AsfBurst, extension) -> String\n\nThe URL of one of a burst's two files, \"tiff\" or \"xml\".\n\n\n\n\n\n","title":"SLCDatasets.asf_burst_url"},{"category":"method","location":"#SLCDatasets.asf_bursts-Tuple{AbstractString, Integer, AbstractString, Any}","page":"Home","text":"asf_bursts(slc, swath, polarization, bursts; orbit, dir = nothing) -> Vector{SLC}\n\nSeveral bursts of one subswath from ASF's burst extractor.\n\nbursts is a range or vector of 1-based burst indices. The metadata file is fetched and parsed once rather than per burst, since each of them carries the whole subswath's annotation; the rasters are fetched as they are read.\n\nThe result merges with merge_bursts.\n\nExamples\n\nb = asf_bursts(\"S1A_IW_SLC__1SSH_20151120T080202_...\", 2, \"HH\", 3:7; orbit = eof)\nref = merge_bursts(b)\n\n\n\n\n\n","title":"SLCDatasets.asf_bursts"},{"category":"function","location":"#SLCDatasets.burst_grid","page":"Home","text":"burst_grid(a::SubswathAnnotation, bursts = 1:nbursts(a); tolerance = MAX_BURST_GRID_RESIDUAL)\n  -> BurstGrid\n\nLay bursts of subswath a out on one uniform azimuth time axis.\n\nbursts must be consecutive and ascending. Row 1 of the grid is the first burst's first line, whether or not that line is valid, so the grid's azimuth origin is that burst's own recorded time.\n\n\n\n\n\n","title":"SLCDatasets.burst_grid"},{"category":"function","location":"#SLCDatasets.burst_index","page":"Home","text":"burst_index(b::AbstractBurstBackend) -> Int\nburst_swath(b::AbstractBurstBackend) -> Int\nburst_polarization(b::AbstractBurstBackend) -> String\nburst_source(b::AbstractBurstBackend) -> String\norbit_path(b::AbstractBurstBackend) -> String\n\nWhich burst of which subswath and channel a backend describes, of which product, with which orbit.\n\nburst_source names the product the burst belongs to, so that bursts of two products are not taken for consecutive bursts of one; the polarization is lowercase.\n\n\n\n\n\n","title":"SLCDatasets.burst_index"},{"category":"function","location":"#SLCDatasets.burst_polarization","page":"Home","text":"burst_index(b::AbstractBurstBackend) -> Int\nburst_swath(b::AbstractBurstBackend) -> Int\nburst_polarization(b::AbstractBurstBackend) -> String\nburst_source(b::AbstractBurstBackend) -> String\norbit_path(b::AbstractBurstBackend) -> String\n\nWhich burst of which subswath and channel a backend describes, of which product, with which orbit.\n\nburst_source names the product the burst belongs to, so that bursts of two products are not taken for consecutive bursts of one; the polarization is lowercase.\n\n\n\n\n\n","title":"SLCDatasets.burst_polarization"},{"category":"method","location":"#SLCDatasets.burst_raster-Tuple{SLCDatasets.AbstractBurstBackend}","page":"Home","text":"burst_raster(b::AbstractBurstBackend) -> BurstRaster\n\nOne burst's samples and where in its raster the burst begins.\n\nWhat a merge asks of each of its sources, so that a burst stacked with its neighbours in a subswath's raster and a burst delivered as its own file are placed the same way. A backend that cannot reach its samples reports that, as read_pixels does.\n\n\n\n\n\n","title":"SLCDatasets.burst_raster"},{"category":"method","location":"#SLCDatasets.burst_rasters-Tuple{AbstractVector{<:SLCDatasets.AbstractBurstBackend}}","page":"Home","text":"burst_rasters(sources::AbstractVector{<:AbstractBurstBackend}) -> Vector{BurstRaster}\n\nThe samples of several bursts of one subswath, opening each file they lie in once.\n\nBursts delivered as separate files have nothing to share and are asked one at a time; bursts stacked in a subswath's raster share it, so a backend whose bursts do gives a method that opens it once.\n\n\n\n\n\n","title":"SLCDatasets.burst_rasters"},{"category":"function","location":"#SLCDatasets.burst_source","page":"Home","text":"burst_index(b::AbstractBurstBackend) -> Int\nburst_swath(b::AbstractBurstBackend) -> Int\nburst_polarization(b::AbstractBurstBackend) -> String\nburst_source(b::AbstractBurstBackend) -> String\norbit_path(b::AbstractBurstBackend) -> String\n\nWhich burst of which subswath and channel a backend describes, of which product, with which orbit.\n\nburst_source names the product the burst belongs to, so that bursts of two products are not taken for consecutive bursts of one; the polarization is lowercase.\n\n\n\n\n\n","title":"SLCDatasets.burst_source"},{"category":"function","location":"#SLCDatasets.burst_swath","page":"Home","text":"burst_index(b::AbstractBurstBackend) -> Int\nburst_swath(b::AbstractBurstBackend) -> Int\nburst_polarization(b::AbstractBurstBackend) -> String\nburst_source(b::AbstractBurstBackend) -> String\norbit_path(b::AbstractBurstBackend) -> String\n\nWhich burst of which subswath and channel a backend describes, of which product, with which orbit.\n\nburst_source names the product the burst belongs to, so that bursts of two products are not taken for consecutive bursts of one; the polarization is lowercase.\n\n\n\n\n\n","title":"SLCDatasets.burst_swath"},{"category":"method","location":"#SLCDatasets.bursts-Tuple{SLCDatasets.AbstractSLCSource}","page":"Home","text":"bursts(src; orbit, swath = 1, polarization = nothing) -> SLCSeries\nbursts(p::Sentinel1Product, swath = first(p.swaths)) -> SLCSeries\n\nEvery burst of one Sentinel-1 subswath, as a vector of SLCs.\n\nThe subswath's annotation is parsed once and shared, so indexing the result costs no further reading of the product — unlike calling open_slc once per burst, which re-reads it each time.\n\nExamples\n\nb = bursts(\"S1A_IW_SLC_....zip\"; orbit = \"...EOF\", swath = 2)\nlength(b)\nb[1].geometry.sensing_start\n[nlines(s) for s in b]\n\n\n\n\n\n","title":"SLCDatasets.bursts"},{"category":"method","location":"#SLCDatasets.default_frequency-Tuple{Any, AbstractString}","page":"Home","text":"default_frequency(h, band) -> String\n\nThe first sub-band a NISAR product lists, as the one to read when the caller names none.\n\n\n\n\n\n","title":"SLCDatasets.default_frequency"},{"category":"method","location":"#SLCDatasets.default_polarization-Tuple{AbstractString}","page":"Home","text":"default_polarization(path) -> String\n\nThe polarization to read when the caller names none.\n\nCo-polarized channels are preferred over cross-polarized ones, and the order otherwise follows testGeogrid.py's getPol, so a dual-polarization product reads the same channel the reference does.\n\n\n\n\n\n","title":"SLCDatasets.default_polarization"},{"category":"method","location":"#SLCDatasets.deramp_parameters-Tuple{SLCDatasets.AbstractSLC}","page":"Home","text":"deramp_parameters(s::SLC)\n\nThe annotation fields a TOPS deramp needs. Not implemented — this throws, naming them.\n\nRemoving a TOPS azimuth ramp needs three things from the Sentinel-1 annotation that this package does not currently parse: the azimuth FM rate polynomials (azimuthFmRateList), the Doppler centroid estimates (dcEstimateList), and the azimuth steering rate (azimuthSteeringRate). They are in the annotation already read for the geometry, so adding them is parsing rather than new IO — but nothing consumes them yet, and a shape guessed ahead of its consumer is worse than an honest gap.\n\nThis exists so that a consumer needing the deramp gets told what is missing and where it lives, rather than discovering it as a wrong answer. See is_tops.\n\n\n\n\n\n","title":"SLCDatasets.deramp_parameters"},{"category":"method","location":"#SLCDatasets.each_overlap-Tuple{Any, SLCDatasets.BurstGrid, AbstractUnitRange{<:Integer}, AbstractUnitRange{<:Integer}}","page":"Home","text":"each_overlap(f, g::BurstGrid, rows, cols)\n\nCall f(k, placement, grid_rows, cols) for each placement of g that a window reaches.\n\ngrid_rows and cols are the part of the window that placement covers, so a caller reads or writes exactly what the placement accounts for and needs no intersection arithmetic of its own. Placements the window misses are skipped, and the corners no placement covers are simply never visited — which is what makes them read as absent.\n\n\n\n\n\n","title":"SLCDatasets.each_overlap"},{"category":"method","location":"#SLCDatasets.epoch_offset-Tuple{RadarGeometry}","page":"Home","text":"epoch_offset(g::RadarGeometry) -> Float64\nepoch_offset(s::SLC) -> Float64\n\nSeconds from midnight of the epoch's own day to the epoch.\n\nRadarGeometry reports its azimuth times against epoch. A consumer measuring them against midnight instead — which is the scale a radar geometry kernel indexes azimuth lines on — needs this constant between the two.\n\nIt is zero for a NISAR product, whose epoch is midnight of the acquisition day, but that is a property of the format rather than a general one.\n\n\n\n\n\n","title":"SLCDatasets.epoch_offset"},{"category":"method","location":"#SLCDatasets.grid-Tuple{SLCDatasets.AbstractSLC}","page":"Home","text":"grid(s::SLC) -> BurstGrid\n\nWhere each burst sits in a merged acquisition.\n\nThrows for an acquisition that is not a merge of bursts.\n\n\n\n\n\n","title":"SLCDatasets.grid"},{"category":"method","location":"#SLCDatasets.grid-Tuple{SLCDatasets.ConcatenatedBursts}","page":"Home","text":"grid(A::ConcatenatedBursts) -> BurstGrid\n\nWhere each burst sits in the merged image.\n\n\n\n\n\n","title":"SLCDatasets.grid"},{"category":"method","location":"#SLCDatasets.is_safe_product-Tuple{AbstractString}","page":"Home","text":"is_safe_product(path) -> Bool\n\nWhether path looks like a Sentinel-1 SAFE product: a .SAFE directory, or a zip holding one.\n\nRecognition is by container shape rather than by file name, since a granule renamed on download is still readable.\n\n\n\n\n\n","title":"SLCDatasets.is_safe_product"},{"category":"method","location":"#SLCDatasets.is_tops-Tuple{SLCDatasets.AbstractSLC}","page":"Home","text":"is_tops(s::SLC) -> Bool\nis_tops(b::AbstractSLCBackend) -> Bool\n\nWhether the acquisition was collected in TOPS mode.\n\nTOPS — Terrain Observation by Progressive Scans, which is how Sentinel-1 IW and EW are acquired — steers the antenna in azimuth across each burst. That puts a steep, burst-varying ramp on the azimuth phase, so the samples are not directly interpolable: a consumer resampling them onto another grid has to remove the ramp first and reapply it on the output, and one that interpolates without doing so aliases it. Amplitudes are unaffected, since taking the magnitude discards the phase.\n\nSo this is the question a consumer of the samples has to ask, and the reason it lives here rather than with the consumer: whether an acquisition is TOPS is a property of how the product was collected, which this package reads and a geometry or correlation package cannot infer.\n\nEvery AbstractBurstBackend is TOPS by construction — the type means \"one burst of a TOPS acquisition\" — and so is a merge of them. A NISAR RSLC is stripmap and is not.\n\nWhat this does not say is whether the ramp has already been removed. A product is delivered ramped; a processor that deramps writes its own product. Were a deramped Sentinel-1 product to arrive here it would still report true, so a consumer holding one has to override rather than ask — see deramp_parameters for what removing the ramp would need, none of which this package yet reads.\n\n\n\n\n\n","title":"SLCDatasets.is_tops"},{"category":"method","location":"#SLCDatasets.localpath-Tuple{LocalFile}","page":"Home","text":"localpath(src::AbstractSLCSource) -> String\n\nA path that can be opened, materializing whatever the source needs to make that true.\n\n\n\n\n\n","title":"SLCDatasets.localpath"},{"category":"method","location":"#SLCDatasets.measurement_path-Tuple{Sentinel1Product, Integer}","page":"Home","text":"measurement_path(product::Sentinel1Product, swath) -> String\n\nThe measurement raster of one subswath.\n\nThrows for a zipped product, naming what to do instead: the raster is deflated inside the archive, so its lines are not addressable without inflating the whole entry.\n\n\n\n\n\n","title":"SLCDatasets.measurement_path"},{"category":"method","location":"#SLCDatasets.merge_bursts-Tuple{AbstractVector{<:SLCDatasets.AbstractSLC}}","page":"Home","text":"merge_bursts(bursts::AbstractVector{<:SLC}; tolerance = MAX_BURST_GRID_RESIDUAL) -> SLC\n\nPlace consecutive bursts of one subswath on a single uniform azimuth grid.\n\nThe bursts must come from one product, subswath and polarization, and be consecutive and in order — which is what bursts returns, so a whole subswath or any slice of one merges directly. The result is an SLC whose nlines spans from the first burst's first line to the last burst's last, with the overlap between neighbours divided at its midpoint rather than repeated.\n\nA line's azimuth time is sensing_start + line / prf throughout the result, which is what RadarGeometry means by prf and what a stack of whole bursts would break at every seam. The samples are placed rather than resampled: each burst's start lands on the shared grid within a fraction of a line, and tolerance is how far it may be off before the merge is refused.\n\nUse pixels or amplitude for the samples, which are read as they are indexed, and validmask for which of them were imaged.\n\nExamples\n\nb = bursts(safe; orbit = eof, swath = 2)\nref = merge_bursts(b)          # the whole subswath\nref = merge_bursts(b[3:7])     # five of its bursts\nnlines(ref)\namplitude(ref)[1:512, 1:512]\n\n\n\n\n\n","title":"SLCDatasets.merge_bursts"},{"category":"method","location":"#SLCDatasets.nbursts-Tuple{Sentinel1Product, Integer}","page":"Home","text":"nbursts(p::Sentinel1Product, swath) -> Int\nnbursts(path; swath = 1, polarization = nothing) -> Int\n\nHow many bursts one subswath of a Sentinel-1 product carries.\n\nNeeded to address bursts by index without opening each in turn, and it reads only the annotation XML.\n\n\n\n\n\n","title":"SLCDatasets.nbursts"},{"category":"method","location":"#SLCDatasets.nisar_band-Tuple{Any}","page":"Home","text":"nisar_band(h) -> String\n\nThe sensor band group of an open NISAR product: \"LSAR\" or \"SSAR\".\n\n\n\n\n\n","title":"SLCDatasets.nisar_band"},{"category":"method","location":"#SLCDatasets.nisar_product_type-Tuple{Any, AbstractString}","page":"Home","text":"nisar_product_type(h, band) -> String\n\nThe product type recorded in an open NISAR product's identification group, as the group name that holds it. Early-mission products name the group SLC where later ones name it RSLC; both report RSLC.\n\n\n\n\n\n","title":"SLCDatasets.nisar_product_type"},{"category":"method","location":"#SLCDatasets.nlines-Tuple{SLC}","page":"Home","text":"nlines(s::SLC)\nnsamples(s::SLC)\n\nImage height and width in pixels.\n\n\n\n\n\n","title":"SLCDatasets.nlines"},{"category":"method","location":"#SLCDatasets.open_slc-Tuple{AsfBurst}","page":"Home","text":"open_slc(b::AsfBurst; orbit) -> SLC\n\nOne ASF-delivered burst as an acquisition.\n\norbit is a POEORB or RESORB .EOF file, required for the same reason it is for a .SAFE: a Sentinel-1 product carries no state vectors.\n\n\n\n\n\n","title":"SLCDatasets.open_slc"},{"category":"method","location":"#SLCDatasets.open_slc-Tuple{SLCDatasets.AbstractSLCSource}","page":"Home","text":"open_slc(src; frequency = nothing, orbit = nothing, swath = nothing, swaths = nothing,\n         burst = nothing, polarization = nothing) -> SLC\n\nOpen a SAR acquisition.\n\nsrc is an AbstractSLCSource, or a path or URL, which is resolved to one. The sensor backend is chosen by inspecting the product rather than by the caller naming it, so the same call serves every supported format.\n\nThe identification and geometry records are read here; the state vectors are read on first access.\n\nNISAR\n\nfrequency selects the sub-band, defaulting to the first the product lists.\n\nSentinel-1\n\norbit is the path to the POEORB or RESORB .EOF file holding the state vectors, and is required: a Sentinel-1 product does not contain its own orbit.\n\nA TOPS product is three subswaths of bursts rather than one image, so there are two geometries to ask for. By default the result spans swaths — all three — the way hyp3-autorift's loadMetadataSlc does. Passing burst instead describes that single burst of the single subswath swath, matching loadMetadata. Use nbursts for the burst count.\n\npolarization defaults to the first of vv, vh, hh, hv the product carries.\n\nExamples\n\ns = open_slc(\"NISAR_L1_PR_RSLC_....h5\")\ns.geometry.prf\n\nmosaic = open_slc(\"S1A_IW_SLC_....zip\"; orbit = \"S1A_OPER_AUX_POEORB_....EOF\")\none    = open_slc(\"S1A_IW_SLC_....zip\"; orbit = \"...EOF\", swath = 2, burst = 3)\n\n\n\n\n\n","title":"SLCDatasets.open_slc"},{"category":"method","location":"#SLCDatasets.open_tiff-Tuple{AbstractString}","page":"Home","text":"open_tiff(path) -> StripedTiff\n\nMemory-map the TIFF at path and read its directory.\n\nThrows unless the file is a classic TIFF whose raster is uncompressed, stored one line per strip, with a single complex 16-bit integer sample per pixel — the layout a Sentinel-1 measurement file has. The raster itself is not read.\n\n\n\n\n\n","title":"SLCDatasets.open_tiff"},{"category":"method","location":"#SLCDatasets.orbit-Tuple{SLC}","page":"Home","text":"orbit(s::SLC) -> StateVectors\n\nThe platform state vectors, read on first call and held afterwards.\n\n\n\n\n\n","title":"SLCDatasets.orbit"},{"category":"function","location":"#SLCDatasets.orbit_path","page":"Home","text":"burst_index(b::AbstractBurstBackend) -> Int\nburst_swath(b::AbstractBurstBackend) -> Int\nburst_polarization(b::AbstractBurstBackend) -> String\nburst_source(b::AbstractBurstBackend) -> String\norbit_path(b::AbstractBurstBackend) -> String\n\nWhich burst of which subswath and channel a backend describes, of which product, with which orbit.\n\nburst_source names the product the burst belongs to, so that bursts of two products are not taken for consecutive bursts of one; the polarization is lowercase.\n\n\n\n\n\n","title":"SLCDatasets.orbit_path"},{"category":"method","location":"#SLCDatasets.parse_cf_epoch-Tuple{AbstractString}","page":"Home","text":"parse_cf_epoch(units) -> DateTime\n\nThe reference instant of a CF-convention units string such as \"seconds since 2025-10-28T00:00:00\".\n\nOnly seconds are accepted: the products this reads record times in seconds, and silently rescaling a different unit would corrupt every time by a constant factor.\n\n\n\n\n\n","title":"SLCDatasets.parse_cf_epoch"},{"category":"method","location":"#SLCDatasets.parse_utc-Tuple{AbstractString}","page":"Home","text":"parse_utc(s) -> UtcTime\n\nThe instant an ISO-8601 timestamp denotes, to the precision it is written with.\n\nYYYY-MM-DDThh:mm:ss with an optional fractional part and an optional trailing zone designator, which is ignored: these products are UTC throughout. The fractional digits are kept in the residual rather than folded into the DateTime, so none are lost.\n\n\n\n\n\n","title":"SLCDatasets.parse_utc"},{"category":"method","location":"#SLCDatasets.path-Tuple{SLCDatasets.StripedTiff}","page":"Home","text":"path(t::StripedTiff) -> String\n\nThe file the raster is mapped from.\n\n\n\n\n\n","title":"SLCDatasets.path"},{"category":"method","location":"#SLCDatasets.pixels-Tuple{SLCDatasets.AbstractSLC}","page":"Home","text":"pixels(s::SLC) -> AbstractMatrix\n\nThe acquisition's samples, as complex values.\n\nThe result is read from the product as it is indexed rather than held, so a subswath costs the windows taken from it. Index it with ranges where the shape of the read allows: p[rows, cols] is one read per burst spanned, while scalar indexing resolves a row per sample.\n\nNot every product can reach its samples. A Sentinel-1 product read from a zip cannot — the raster is deflated inside it — and neither can a remote product whose image was never transferred; both say so rather than returning something partial. amplitude for the real magnitudes, and validmask for which samples were imaged.\n\n\n\n\n\n","title":"SLCDatasets.pixels"},{"category":"method","location":"#SLCDatasets.placement_at-Tuple{SLCDatasets.BurstGrid, Integer}","page":"Home","text":"placement_at(g::BurstGrid, row) -> Union{BurstPlacement,Nothing}\n\nThe placement covering row, or nothing where no burst reaches it.\n\n\n\n\n\n","title":"SLCDatasets.placement_at"},{"category":"method","location":"#SLCDatasets.placement_index-Tuple{SLCDatasets.BurstGrid, Integer}","page":"Home","text":"placement_index(g::BurstGrid, row) -> Int\n\nWhich of g's placements covers row, or 0 where no burst reaches it.\n\nThe placements ascend and do not overlap, so the one that could cover a row is the last one starting at or before it; whether it does is one bounds check on that placement.\n\n\n\n\n\n","title":"SLCDatasets.placement_index"},{"category":"method","location":"#SLCDatasets.read_annotation_entry-Tuple{SLCDatasets.AnnotationTree, Integer, AbstractString}","page":"Home","text":"read_annotation_entry(r::AnnotationTree, swath, polarization) -> String\n\nThe annotation XML of one subswath and polarization, from an already-listed container.\n\n\n\n\n\n","title":"SLCDatasets.read_annotation_entry"},{"category":"method","location":"#SLCDatasets.read_eof_state_vectors-Tuple{AbstractString}","page":"Home","text":"read_eof_state_vectors(path; from = nothing, to = nothing, padding = 0.0) -> StateVectorTable\n\nThe orbit state vectors of a POEORB or RESORB .EOF file.\n\nWith from and to given, only the vectors within padding seconds of that window are kept, and the scan stops once past it. A POEORB covers just over a day at one vector every ten seconds while an acquisition needs a few, so filtering during the scan rather than after it is what keeps reading one cheap: a record whose time falls outside the window has its six numbers skipped entirely.\n\n\n\n\n\n","title":"SLCDatasets.read_eof_state_vectors"},{"category":"method","location":"#SLCDatasets.read_pixels-Tuple{SLCDatasets.AbstractSLCBackend}","page":"Home","text":"read_pixels(b::AbstractSLCBackend) -> AbstractMatrix\n\nThe samples of the product b reads, for a backend that can reach them.\n\nThe optional half of the backend protocol: a backend that reads metadata alone leaves this unimplemented, and pixels then reports that rather than failing on dispatch.\n\n\n\n\n\n","title":"SLCDatasets.read_pixels"},{"category":"method","location":"#SLCDatasets.repeat_interval-Tuple{SLCDatasets.AbstractSLC, SLCDatasets.AbstractSLC}","page":"Home","text":"repeat_interval(reference::SLC, secondary::SLC) -> Float64\n\nSeconds from the reference acquisition's start to the secondary's.\n\nMeasured between the two epochs as well as the two azimuth times, so a pair of products carrying different epochs — which two acquisitions weeks apart always do — is differenced on one scale rather than silently on two.\n\nExamples\n\nrepeat_interval(a, b) / 86400   # the interval in days\n\n\n\n\n\n","title":"SLCDatasets.repeat_interval"},{"category":"method","location":"#SLCDatasets.s3_https_url-Tuple{AbstractString}","page":"Home","text":"s3_https_url(uri; region = nothing) -> String\n\nThe virtual-hosted HTTPS endpoint of an s3://bucket/key URI.\n\nregion defaults to AWS_REGION, then AWS_DEFAULT_REGION. A bucket reached through the wrong region's endpoint is refused by S3 with a redirect rather than served, so the region has to be right.\n\n\n\n\n\n","title":"SLCDatasets.s3_https_url"},{"category":"method","location":"#SLCDatasets.safe_polarizations-Tuple{AbstractString}","page":"Home","text":"safe_polarizations(path) -> Vector{String}\nsafe_polarizations(r::AnnotationTree) -> Vector{String}\n\nThe polarizations a SAFE product carries, lowercase, in the order the annotation files sort.\n\n\n\n\n\n","title":"SLCDatasets.safe_polarizations"},{"category":"method","location":"#SLCDatasets.seconds_between-Tuple{SLCDatasets.UtcTime, SLCDatasets.UtcTime}","page":"Home","text":"seconds_between(from::UtcTime, to::UtcTime) -> Float64\n\nSeconds from one UTC instant to another.\n\nThe whole-second difference is exact in Int64 milliseconds and the sub-second parts are added afterwards, so the result does not inherit the rounding a DateTime-only subtraction would introduce.\n\n\n\n\n\n","title":"SLCDatasets.seconds_between"},{"category":"method","location":"#SLCDatasets.source_for-Tuple{AbstractString}","page":"Home","text":"source_for(spec) -> AbstractSLCSource\n\nThe source a path, URL or S3 URI denotes.\n\n\n\n\n\n","title":"SLCDatasets.source_for"},{"category":"method","location":"#SLCDatasets.start_datetime-Tuple{Identification}","page":"Home","text":"start_datetime(id::Identification) -> DateTime\n\nThe acquisition's zero-Doppler start as a DateTime, truncated to milliseconds.\n\nThe product records it to nanoseconds. Use id.start_time for the exact string, or RadarGeometry's sensing_start for full-precision arithmetic.\n\n\n\n\n\n","title":"SLCDatasets.start_datetime"},{"category":"method","location":"#SLCDatasets.stop_datetime-Tuple{Identification}","page":"Home","text":"stop_datetime(id::Identification) -> DateTime\n\nThe acquisition's zero-Doppler end as a DateTime, truncated to milliseconds.\n\nThe counterpart of start_datetime, with the same caveat: the product records it to nanoseconds, and RadarGeometry's sensing_stop carries the full precision.\n\n\n\n\n\n","title":"SLCDatasets.stop_datetime"},{"category":"method","location":"#SLCDatasets.valid_lines-Tuple{SLCDatasets.SubswathAnnotation, Integer}","page":"Home","text":"valid_lines(a::SubswathAnnotation, burst) -> UnitRange{Int}\nvalid_samples(a::SubswathAnnotation, burst) -> UnitRange{Int}\n\nThe rows and columns of one burst that carry data, as 1-based inclusive ranges.\n\nA burst's raster is lines_per_burst by samples_per_burst, but only this sub-rectangle of it was imaged; the margin outside carries whatever the processor left there, which is not zero. So a reader combining bursts must clip to these, and a caller reading a burst's raster directly gets the margin unless it does.\n\n\n\n\n\n","title":"SLCDatasets.valid_lines"},{"category":"method","location":"#SLCDatasets.validmask-Tuple{SLCDatasets.AbstractSLC}","page":"Home","text":"validmask(s::SLC) -> AbstractMatrix{Bool}\n\nWhich of the acquisition's samples were imaged.\n\nfalse where no data was recorded — the corners a merged subswath leaves, and the margin around each burst's valid region. A sample that was imaged may still be zero, so this is the honest test for absence rather than comparing the samples against zero.\n\nPass it alongside the samples to a consumer that takes a validity mask, rather than letting it derive one: it is computed from the layout and reads nothing.\n\n\n\n\n\n","title":"SLCDatasets.validmask"},{"category":"method","location":"#SLCDatasets.validmask-Tuple{SLCDatasets.ConcatenatedBursts}","page":"Home","text":"validmask(A) -> AbstractMatrix{Bool}\n\nWhich samples of A were imaged.\n\nFor a merged subswath this is BurstValidMask, computed from the burst placements and holding nothing, so asking for it costs no read of the rasters.\n\n\n\n\n\n","title":"SLCDatasets.validmask"},{"category":"method","location":"#SLCDatasets.with_annotations-Tuple{Any, AbstractString}","page":"Home","text":"with_annotations(f, path)\n\nCall f on the AnnotationTree of the SAFE product at path, however it is packaged.\n\n\n\n\n\n","title":"SLCDatasets.with_annotations"}]
}
